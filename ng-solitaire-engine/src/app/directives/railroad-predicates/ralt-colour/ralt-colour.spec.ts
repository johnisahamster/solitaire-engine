import { RAltColour } from './ralt-colour';

describe('RAltColour', () => {
  it('should create an instance', () => {
    const directive = new RAltColour();
    expect(directive).toBeTruthy();
  });
});
